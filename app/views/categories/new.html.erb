<%= form_for @category, url: {action: "create", controller: 'categories'} do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <%= f.submit 'Create' %>
<% end %>
